name: toolroom

services:
  app:
    image: toolroom:local
    build:
      context: .
      target: runtime
    ports:
      - "3011:8080"
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
